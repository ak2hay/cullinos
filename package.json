{
  "name": "cullinos",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:report": "playwright show-report",
    "test:e2e:ui": "playwright test --ui",
    "db:generate": "npm run generate -w @cullinos/prisma",
    "db:push": "npm run push -w @cullinos/prisma",
    "db:migrate": "npm run migrate -w @cullinos/prisma",
    "db:seed": "npm run seed -w @cullinos/prisma",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f docker/docker-compose.yml down",
    "build:api": "npm run db:generate && npx turbo run build --filter=@cullinos/api"
  },
  "dependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@tailwindcss/vite": "^4.0.0",
    "dotenv": "^16.6.1",
    "tailwindcss": "^4.0.0",
    "vitest": "^2.1.8"
  },
  "packageManager": "npm@10.9.2",
  "overrides": {
    "@nestjs/common": "11.2.3",
    "@nestjs/core": "11.2.3",
    "@nestjs/platform-express": "11.2.3",
    "@nestjs/websockets": "11.2.3",
    "@nestjs/platform-socket.io": "11.2.3"
  }
}
