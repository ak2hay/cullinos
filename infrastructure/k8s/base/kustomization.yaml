apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - config.yaml
  - postgres.yaml
  - redis.yaml
  - api.yaml
  - web.yaml
  - spas.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: cullinos
    includeSelectors: false
