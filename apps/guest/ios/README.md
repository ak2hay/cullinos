# iOS project placeholder
#
# After installing Flutter SDK:
#   cd apps/guest && flutter create . --platforms=ios,android
# This regenerates missing platform scaffolding while keeping lib/ sources.
#
# v1 Play Store launch is Android-only; keep iOS buildable for later App Store work.
