{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["npm run build:api"]
    }
  },
  "deploy": {
    "startCommand": "node index.js"
  }
}
